- name: Demo on loops
  hosts: localhost
  connection: local
  tasks:
    - name: print fruit name
      ansible.builtin.debug:
        msg: fruitName - {{ item }}
      loop:
        - apple
        - orange
        - grape
        - banana
